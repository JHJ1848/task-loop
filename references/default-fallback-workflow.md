# [Reference] 专题会话默认兜底工作流规范 (Default Fallback Workflow)

本文档定义了 `task-loop` 各专题会话在**无第三方工作流插件**（如 `jiaohuan-dev` 等）环境下的原生默认执行工作流，确保在纯净、零依赖环境下保持极简、高效、安全的代码实施闭环。

---

## 一、双分支执行决策与子代理派遣智能判断门槛 (Decision Matrix & Parallelism Gate)

当任务到达专题会话后，专题负责人依据任务特征与并发价值进行快速分流：

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        专题会话任务接入与调度判断                      │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
       ┌────────────────────────────┴────────────────────────────┐
       ▼                                                         ▼
【模式 A: 专题会话单线程就地执行】              【模式 B: 派发多子代理并发协同】
 (满足以下任一即走单线程就地执行):                (必须满足以下严格门槛才允许派遣):
 1. 目标简单明确 (Simple & Clear)                 1. 并行度 >= 2: 任务可解耦为至少 2 个
 2. 单线探索 / 单文件修改 / 串行依赖                独立无依赖的子任务 (2~3 并发加速)
 3. 无物理强隔离沙箱需求                          2. 物理强隔离: 高风险/破坏性实验需要
                                                    Workspace='branch' 沙箱保护主工作区
                                                         │
                                                         ▼
                                                启动 explore / worker / viewer 并行协同
```

```json
[
  {
    "pattern": "反模式警示: 严禁单子代理串行干等 (Anti-Pattern: Single-Subagent Stalling)",
    "hazard": "如果拆解后仅下发 1 个子代理，父会话陷入阻塞挂起 (干等)，不仅零并发加速收益，反而徒增子代理拉起、上下文重复打包、跨进程通信与唤醒延迟，严重拖垮研发进度并浪费 Token。",
    "hard_rule": "严禁派发单一子代理处理普通的串行单线任务！无并发收益或无沙箱需求时，专题会话自身单线程直接执行必定最快、最省、最准。"
  },
  {
    "branch": "模式 A: 专题会话单线程就地执行 (Local Direct Execution)",
    "condition": "目标简单明确，或虽复杂但属于单线串行任务、无需物理沙箱隔离",
    "workflow": "专题会话自身直接调用只读检索工具探测、在 Allowlist 白名单内修改代码、运行本地单元测试 (Exit Code 0)，直接交付闭环。最纯洁极速，零进程切换开销。"
  },
  {
    "branch": "模式 B: 多子代理并发协作流 (Parallel Subagents Workflow)",
    "condition": "任务可明确拆解为 >= 2 个可并行推进的独立工作单元，或存在破坏性沙箱隔离需求",
    "workflow": "在满足并发度 >= 2 门槛时，依次或并发调度 explore (探测) -> worker (实施) -> viewer (审查) 子代理协同推进 (各角色并发建议 2~3 个)。"
  }
]
```

---

## 二、三角色子代理协作与并发度门槛 (Explore + Worker + Viewer)

针对模式 B（满足并发度 >= 2 或强沙箱隔离的复杂任务），专题会话作为二级调度中枢按流水线推进：

```text
┌──────────────────────────────────────────────────────────────────────────┐
│                   三角色子代理标准协作流水线                             │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ 1. [explore 探测 (>= 2 并发)] 多源/多模块同时独立排查现状与根因          │
│         │ (注: 若仅需探测单个模块，专题会话自己读，严禁派发单个 explore) │
│         ▼                                                                │
│ 2. [专题会话统筹] 汇总并发探测结论，规划实施方案与白名单边界             │
│         │                                                                │
│         ▼                                                                │
│ 3. [worker 实施 (>= 2 并发)] 多个解耦模块并行实施代码修改与本地单测      │
│         │ (注: 若为单点或前后依赖修改，专题会话自己写，严禁派发单个 worker) │
│         ├─► (中途异常中断机制: 发现意外/额外逻辑 -> 立即停止并反馈专题会话)│
│         │                                                                │
│         ▼                                                                │
│ 4. [viewer 审查 (可选并发)] 对修改结果与测试证据进行独立客观评判         │
│         │                                                                │
│         ▼                                                                │
│ 5. [专题会话判定] 评估审查结果:                                          │
│         ├─► 评判通过: 交付闭环                                           │
│         └─► 评判未通过 (短路迭代): 直接通知 worker 针对性改动 (不重新explore)
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### 1. 角色职责与并发度智能判断准则 (Parallelism Criteria)
每个阶段下发子代理时，必须严格通过并发度智能判断：

```json
[
  {
    "role_name": "explore",
    "dispatch_condition": "仅当需要同时探测 2 个以上异构模块、对比多个不同方案或检索多份独立日志源时派发 (2~3 个并发)；若仅排查单一调用链，由专题会话自身直接读取，绝不下发单个 explore 干等。",
    "recommended_limit": "2 ~ 3 个并发",
    "role_type": "只读探测 (Read-Only Explorer)",
    "recommended_model": "flash (或 inherit)"
  },
  {
    "role_name": "worker",
    "dispatch_condition": "仅当存在 2 个以上物理文件集互不重叠 (Allowlist 互斥解耦)、可完全独立并发编码的子任务时派发 (2~3 个并发)；若代码改动具有先后依赖关系或仅涉及单模块，由专题会话自身直接修改，绝不下发单个 worker 干等。",
    "recommended_limit": "2 ~ 3 个并发",
    "role_type": "编码实施 (Implementer)",
    "recommended_model": "inherit (或 flash)"
  },
  {
    "role_name": "viewer",
    "dispatch_condition": "当需要同时开展不同维度的交叉审查 (如安全漏洞审查 + 业务规范走查 2 个并发) 时派发；常规审查由专题会话自身核对测试日志与 Diff，绝不下发单个 viewer 干等。",
    "recommended_limit": "1 ~ 2 个",
    "role_type": "客观评判 (Reviewer)",
    "recommended_model": "pro (或 inherit)"
  },
  {
    "role_name": "sandbox_worker (特例)",
    "dispatch_condition": "单任务破坏性实验特许: 即使只有 1 个任务，但属于高风险原型、脏环境测试或易崩溃重构，必须借助 Workspace='branch' 物理隔离保护主工作区时，特许派发 1 个沙箱代理。",
    "recommended_limit": "1 个 (沙箱专用)",
    "role_type": "沙箱隔离实施 (Sandbox Worker)",
    "recommended_model": "inherit"
  }
]
```

### 2. 短路重试机制 (Short-Circuit Iteration)
* **核心原则**：当 `viewer` 评判未通过或自测存在缺陷时，**专题会话直接将失败点派发给原 `worker` 进行针对性修改，或由专题会话就地微调修复**。
* **效率保证**：严禁在评审失败后重新走长链路退回 `explore`，避免重复的探索成本，确保开发流转极速收敛。

### 3. 中途异常中断与统筹机制 (Mid-Flight Escalation Guardrail)
* **核心原则**：`worker` 在实施过程中，一旦发现以下意外情况，**必须立即停止当前修改并向专题会话反馈**：
  1. 发现历史遗留冲突或未预期的隐藏业务逻辑；
  2. 发现修改会导致其他核心模块编译或测试连锁崩溃；
  3. 发现实际修改范围必须超出原定 `Allowlist` 物理白名单。
* **处理闭环**：专题会话接收到异常反馈后，重新进行全局统筹评估并明确新方案，再指派 `worker` 继续往下推进。

---

## 三、第三方插件插槽协同 (Third-Party Plugin Interoperability)

```json
[
  {
    "scenario": "未安装第三方插件 (默认纯净环境)",
    "action": "100% 严格执行本文档定义的【简单直接改动 / 复杂 explore + worker + viewer】原生兜底流程。"
  },
  {
    "scenario": "已安装第三方插件 (如 jiaohuan-dev)",
    "action": "可按需结合其 /project-memory, /debug, /dev 等方法论，但核心协作流与短路重试机制依然有效。"
  }
]
```

---

## 四、双向关联索引
* **受控记忆主纲要**: [`docs/MEMORY.md`](../docs/MEMORY.md)
* **子代理专题受控记忆**: [`docs/memory/subagent.md`](../docs/memory/subagent.md)
* **项目根规则**: [`AGENTS.md`](../AGENTS.md)
* **派单与治理契约**: [`references/dispatch-contract.md`](dispatch-contract.md)
